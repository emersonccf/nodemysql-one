const mysql = require('mysql')

const connection = mysql.createConnection({
    host: "localhost",
    port: 3306,
    user: 'root',
    password: '6932',
    database: 'sefit_teste' // ajustar
})

connection.connect(function(err){
    if(err) return console.log(err)
    console.log('Conexão realizada com sucesso')
})
